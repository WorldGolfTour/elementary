# Snowflake "dbt Projects" compile rejects Hub-style `package:` entries in nested deps.
# Pin dbt_utils via git + revision (same as typical lock resolution ~1.3.x).
packages:
  - git: "https://github.com/dbt-labs/dbt-utils.git"
    revision: "1.3.3"
